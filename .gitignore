# Generated local data
lancedb/

# Temporary uv files
uv-*.lock

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*~
.gstack/

# Upstream beets clone (worked on separately; not tracked in this docs repo)
beets/

# Raw command logs used to render screenshots
docs/screenshots/.raw/

# pytest junit output (written to repo root by beets' setup.cfg)
.reports/

# Local AI tooling config (context-mode routing) — redundant with the global
# ~/.claude/CLAUDE.md; kept locally but not tracked in this contribution repo
CLAUDE.md
